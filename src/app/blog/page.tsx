import { PlaceholderPage } from "@/components/shared/PlaceholderPage";

export default function BlogPage() {
  return (
    <PlaceholderPage
      eyebrow="Blog"
      title="Editorial content starts here."
      description="The blog shell is ready for SEO-focused expat guides, country explainers, and launch posts in later phases."
      primaryCtaHref="/"
      primaryCtaLabel="Back to home"
      secondaryCtaHref="/countries"
      secondaryCtaLabel="Browse countries"
    />
  );
}
